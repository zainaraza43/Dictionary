
public class HashTableInternal {
	//TODO: appropriate data structure for hash table.
	
	public HashTableInternal(InternalList internalList) {
		// TODO: This constructor should read an InternalList object and convert it to a hash table.
		// TODO: Please new the hash table, then call InternalList.getWord() to get input value.
	}
	
	public int hashFunction(String word) {
		//TODO: Convert each Word.getWord() to its hash value.
		return 0;
	}
	
	public Word searchWord(String word) {
		//TODO: call hashFunction() to get its hash value, then use this hash value to get the Word object from the hash talbe.
		return null;
	}
}
